{
  "name": "docs",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "bun build --compile src/index.ts --outfile hono-server",
    "buildLinux": "bun build ./src/index.ts --compile --target=bun-linux-x64 --outfile ./my-linux-app"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "better-sqlite3": "^12.4.1",
    "hono": "^4.10.3"
  }
}
